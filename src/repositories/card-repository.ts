import { prisma } from "../config/database";

/* async function findSessionCards(userId: string, session: string) {
    const sessionCards = prisma.card.findMany({
        where: {
            OR: [
                {

                }
            ]
        }
    })
} */